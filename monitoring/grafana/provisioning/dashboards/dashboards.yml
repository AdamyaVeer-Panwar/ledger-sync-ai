apiVersion: 1

providers:
  - name: LedgerSync
    orgId: 1
    folder: LedgerSync AI
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true