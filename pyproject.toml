[project]
name = "ledger-sync-ai"
version = "0.1.0"
description = "AI-assisted financial reconciliation controller"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]